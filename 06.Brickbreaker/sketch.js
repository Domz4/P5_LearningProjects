function setup () {
  createCanvas(1200,800);
}
function draw () {
  background(0);
  fill(255);
  rectMode(CENTER);
  rect(mouseX,height-20,150,20);
}
class Ball {
  constructor(x,y,r) {
    this.x = x
    this.y = y
    this.r = r
}
move()



}
